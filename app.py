import streamlit as st
import os

# إعدادات الصفحة
st.set_page_config(page_title="FlashDeal - Talk. Pay. Done.", layout="centered")

# شعار المشروع
st.title("⚡ FlashDeal")
st.subheader("Talk. Pay. Done.")

# القسم القانوني وإخلاء المسؤولية (شرط أساسي للأمان)
st.info("إقرار أمني وقانوني")
agreed = st.checkbox("أقر بأنني المسؤول الأول عن العمليات المالية المنفذة وأوافق على شروط الاستخدام.")

if agreed:
    st.success("نظام الأمان الطبقي نشط الآن ✅")
    
    # عرض الفيديو التوضيحي للمشروع
    video_file = "1000021234.mp4"
    if os.path.exists(video_file):
        st.video(video_file)
    else:
        st.warning("يرجى التأكد من رفع ملف الفيديو بنفس المجلد.")

    # واجهة التفاعل (محاكاة وكلاء Gemini)
    st.write("---")
    user_input = st.text_input("قل شيئاً (مثال: اشترِ العرض الصاعق الآن)")
    
    if user_input:
        st.write("🔄 جاري معالجة الطلب عبر وكيل Gemini الذكي...")
        # نظام التدخل عند الريبة
        if "شراء" in user_input or "pay" in user_input.lower():
            st.warning("⚠️ تأكيد إضافي مطلوب: هل أنت متأكد من تنفيذ هذه العملية بقيمة [X]؟")
            if st.button("تأكيد الدفع النهائي"):
                st.balloons()
                st.success("تمت العملية بنجاح! Done. 🎉")
else:
    st.warning("يرجى الموافقة على الإقرار الأمني لتفعيل واجهة FlashDeal.")

# تذييل الصفحة
st.caption("FlashDeal © 2026 - Powered by Gemini AI Agents")
